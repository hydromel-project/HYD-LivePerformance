# =============================================================================
# HYD-PlayrateBot .gitignore
# (For standalone use - parent .gitignore also applies when in main repo)
# =============================================================================

# Dependencies
node_modules/

# Config with credentials (Twitch, Streamlabs, StreamElements tokens)
config.json

# Environment files
.env
.env.*
!.env.example

# Runtime data (contains user info)
requests_log.json
requests_stats.json
song_library.json

# Logs
*.log
